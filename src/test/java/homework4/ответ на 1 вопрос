Ответ: да, разница есть.
 -$("h1 div") - будет искаться первый элемент div, у которого в предках есть h1.
  то есть будет искаться первая комбинация таких элементов по всему дому.
  упадет с ошибкой, если не будет будет такого сочетания во всем доме.
 -$("h1").$("div") - будет искаться первый h1, а внутри h1 уже первый  div.
 упадет с ошибкой, если div не будет в первом h1.

 может ли привести к тому что, поиск найдёт разные элементы?
  В данном случа не может, либо найдется один элемент, либо одна из команд упадет с ошибкой.